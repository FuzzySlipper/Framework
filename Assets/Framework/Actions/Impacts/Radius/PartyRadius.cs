using UnityEngine;
using System.Collections;
using System.Collections.Generic;

namespace PixelComrades {
    public class PartyRadius : IRadiusHandler {

        public void HandleRadius(Entity owner, Entity originalTarget) {
            //for (int i = 0; i < Player.Party.Length; i++) {
            //    if (Player.Party[i] == alreadyHit) {
            //        continue;
            //    }
            //    ApplyRadiusCollision(Player.Party[i], center, originalEvent);
            //}
        }
    }
}

using UnityEngine;
using System.Collections;
using System.Collections.Generic;

namespace PixelComrades {
    public class LineRadius : IRadiusHandler {

        public void HandleRadius(Entity owner, Entity originalTarget) {
            //var list = TargettingType == TargetType.Friendly ? Actor.GetFactionList(Owner.Faction) : Actor.GetEnemies(Owner.Faction);
            //var row = alreadyHit.PositionRank;
            //for (int i = 0; i < list.Count; i++) {
            //    var actor = list[i];
            //    if (actor != alreadyHit && actor.PositionRank == row) {
            //        ApplyRadiusCollision(actor, center, originalEvent);
            //    }
            //}
        }
    }
}

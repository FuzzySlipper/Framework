using UnityEngine;
using System.Collections;
using System.Collections.Generic;

namespace PixelComrades {
    public class PartyRadius : IRadiusHandler {

        public void HandleRadius(Entity owner, Entity originalTarget) {
            //for (int i = 0; i < PlayerPartySystem.Party.Length; i++) {
            //    if (PlayerPartySystem.Party[i] == alreadyHit) {
            //        continue;
            //    }
            //    ApplyRadiusCollision(PlayerPartySystem.Party[i], center, originalEvent);
            //}
        }
    }
}
